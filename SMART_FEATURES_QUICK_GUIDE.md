# 🎯 دليل سريع - كيفية استخدام المميزات الذكية

## 🚀 الوصول السريع

1. **افتح**: http://127.0.0.1:8000/magic-login (تسجيل دخول تلقائي)
2. **اذهب إلى**: http://127.0.0.1:8000/smart-features

---

## ✅ المميزات المتوفرة الآن

### 1️⃣ التنبيهات والتحذيرات ✅

**APIs الجاهزة:**
```
GET  /api/alerts              - قائمة التنبيهات
GET  /api/alerts/unread-count - عدد غير المقروءة
POST /api/alerts/{id}/read    - قراءة تنبيه
POST /api/alerts/{id}/dismiss - إخفاء تنبيه
POST /api/alerts/read-all     - قراءة الكل
POST /api/alerts/run-checks   - فحص التنبيهات الآن ⭐
```

**أنواع التنبيهات:**
- ✅ جرعات متقاربة (< 2 ساعات)
- ✅ جرعات منسية (> 2 ساعات تأخير)
- ✅ جرعات متأخرة (حتى ساعتين)
- ✅ أدوية متعددة في نفس الوقت
- ✅ نفاد مخزون الدواء

**للاختبار:**
1. أضف أدوية من http://127.0.0.1:8000/medicines
2. افتح صفحة المميزات الذكية
3. اضغط "فحص التنبيهات الآن"

---

### 2️⃣ تنبيه نفاد الدواء ✅

**APIs الجاهزة:**
```
GET  /api/inventory               - حالة جميع الأدوية
GET  /api/inventory/low-stock     - الأدوية المنخفضة
POST /api/inventory/check         - فحص النواقص ⭐
GET  /api/medicines/{id}/inventory - حالة دواء محدد
POST /api/medicines/{id}/refill    - تسجيل تعبئة ⭐
POST /api/medicines/{id}/update-quantity - تحديث الكمية
```

**الميزات:**
- ✅ حساب الكمية المتبقية تلقائياً
- ✅ حساب عدد الأيام حتى النفاد
- ✅ تنبيهات عند انخفاض المخزون
- ✅ تتبع جميع عمليات التعبئة

**للاختبار:**
1. أضف دواء مع كمية حالية (مثل: 20 حبة)
2. حدد حد التنبيه (مثل: 5 حبات)
3. اضغط "فحص النواقص"

---

### 3️⃣ نظام المرافقين (أفراد العائلة) ✅

**APIs الجاهزة:**
```
GET  /api/companions              - قائمة المرافقين
POST /api/companions/invite       - دعوة مرافق ⭐
POST /api/companions/{token}/accept - قبول دعوة
POST /api/companions/{token}/reject - رفض دعوة
PUT  /api/companions/{id}/permissions - تحديث الصلاحيات
DELETE /api/companions/{id}       - إزالة مرافق
GET  /api/companions/patients     - المرضى المتابعين
```

**مثال - دعوة مرافق:**
```bash
curl -X POST http://127.0.0.1:8000/api/companions/invite \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "email": "companion@example.com",
    "relationship": "mother",
    "permissions": ["view_medications", "view_doses", "receive_alerts"]
  }'
```

---

### 4️⃣ مشاركة خطة العلاج ✅

**APIs الجاهزة:**
```
GET  /api/treatment-plan/shares   - قائمة المشاركات
POST /api/treatment-plan/share    - إنشاء رابط مشاركة ⭐
GET  /api/shared/treatment-plan/{token} - عرض الخطة (عام)
POST /api/treatment-plan/shares/{id}/deactivate - تعطيل رابط
DELETE /api/treatment-plan/shares/{id} - حذف رابط
```

**مثال - إنشاء رابط مشاركة:**
```bash
curl -X POST http://127.0.0.1:8000/api/treatment-plan/share \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "recipient_email": "doctor@example.com",
    "recipient_name": "Dr. Ahmed",
    "format": "web",
    "expires_days": 7,
    "notes": "خطة العلاج للمراجعة"
  }'
```

ستحصل على:
- ✅ رابط آمن للمشاركة
- ✅ صلاحية محددة (7 أيام)
- ✅ تتبع عدد المشاهدات

---

## 🧪 اختبار سريع

### استخدام cURL:

```bash
# 1. تسجيل الدخول
curl -X POST http://127.0.0.1:8000/api/login \
  -H "Content-Type: application/json" \
  -d '{"email":"magic@app.com","password":"Super$trong!P@ss2025"}'

# احفظ الـ token

# 2. فحص التنبيهات
curl http://127.0.0.1:8000/api/alerts \
  -H "Authorization: Bearer YOUR_TOKEN"

# 3. تشغيل فحص التنبيهات
curl -X POST http://127.0.0.1:8000/api/alerts/run-checks \
  -H "Authorization: Bearer YOUR_TOKEN"

# 4. فحص المخزون
curl http://127.0.0.1:8000/api/inventory \
  -H "Authorization: Bearer YOUR_TOKEN"

# 5. فحص النواقص
curl -X POST http://127.0.0.1:8000/api/inventory/check \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 📊 ما تم إنجازه - ملخص نهائي

| الميزة | Database | Models | Services | APIs | الحالة |
|--------|:--------:|:------:|:--------:|:----:|:------:|
| 🔔 التنبيهات والتحذيرات | ✅ | ✅ | ✅ | ✅ | **100%** |
| 📦 تنبيه نفاد الدواء | ✅ | ✅ | ✅ | ✅ | **100%** |
| 👥 نظام المرافقين | ✅ | ✅ | ⚠️ | ✅ | **90%** |
| 📤 مشاركة خطة العلاج | ✅ | ✅ | ⚠️ | ✅ | **90%** |
| 🧠 التذكير الذكي | ✅ | ⏳ | ⏳ | ⏳ | **25%** |
| 🔍 بحث خارجي | ⏳ | ⏳ | ⏳ | ⏳ | **0%** |
| 📴 استخدام بدون إنترنت | ⏳ | ⏳ | ⏳ | ⏳ | **0%** |

**الإجمالي: 60% من المميزات مكتملة وجاهزة للاستخدام! 🎉**

---

## 🎯 لماذا قد لا تظهر المميزات؟

### السبب 1: لا توجد بيانات
- **الحل**: أضف أدوية من صفحة الأدوية
- **الحل**: سجل جرعات
- **الحل**: اضغط "فحص التنبيهات الآن"

### السبب 2: الملفات الجديدة تحتاج إعادة تحميل
- **الحل**: أعد تحميل الصفحة (Ctrl+F5)
- **الحل**: امسح الـ cache

### السبب 3: المميزات APIs فقط
- بعض المميزات (مثل المرافقين) APIs فقط
- تحتاج واجهة مستعلى (Frontend) لتظهر بالكامل

---

## 💡 نصيحة سريعة

**لرؤية النتائج فوراً:**

1. افتح http://127.0.0.1:8000/magic-login
2. اذهب لصفحة الأدوية وأضف دواء
3. افتح http://127.0.0.1:8000/smart-features
4. اضغط "فحص التنبيهات الآن"
5. اضغط "فحص النواقص"

---

## 📞 للمزيد

اقرأ: `SMART_FEATURES_STATUS.md` للتفاصيل الكاملة
