# 🧠 خطة المميزات الذكية - Medication Reminder

## نظرة عامة

هذا المستند يحدد خطة تنفيذ المميزات الذكية الجديدة للتطبيق.

---

## 1️⃣ التذكير الذكي بالدواء

### الوصف
التطبيق يتعلم من سلوك المستخدم ويقترح أوقات أفضل للتذكير.

### التنفيذ

#### Database Schema
```sql
- doses table: أضف `suggested_time`, `is_suggestion_accepted`
- user_behavior_analytics table: تتبع أوقات التأخير
```

#### Backend Components
- `SmartSchedulingService` - تحليل البيانات واقتراح الأوقات
- `BehaviorAnalyzer` - تحليل سلوك المستخدم
- `DoseSuggestionJob` - مهمة دورية لتحليل واقتراح

#### API Endpoints
- `GET /api/dose-suggestions` - الحصول على الاقتراحات
- `POST /api/dose-suggestions/{id}/accept` - قبول الاقتراح
- `POST /api/dose-suggestions/{id}/decline` - رفض الاقتراح

---

## 2️⃣ تنبيه قبل نفاد الدواء

### الوصف
حساب الكمية المتبقية وتنبيه المستخدم قبل نفاد الدواء.

### التنفيذ

#### Database Schema
```sql
- medicines table: أضف `current_quantity`, `quantity_unit`, `min_quantity_alert`
- medication_refills table: سجل عمليات التعبئة
```

#### Backend Components
- `InventoryService` - إدارة المخزون
- `LowStockAlertJob` - فحص المخزون يومياً
- `MedicationRefillNotification` - إشعار نفاد الدواء

#### API Endpoints
- `GET /api/medicines/{id}/inventory` - حالة المخزون
- `POST /api/medicines/{id}/refill` - تسجيل تعبئة جديدة
- `GET /api/medicines/low-stock` - قائمة الأدوية المنخفضة

---

## 3️⃣ الاستخدام بدون إنترنت

### الوصف
PWA كامل مع Service Workers للعمل بدون اتصال.

### التنفيذ

#### Frontend Components
- Service Worker للتخزين المؤقت
- IndexedDB للبيانات المحلية
- Sync API للمزامنة التلقائية

#### Backend Components
- `SyncQueueService` - معالجة طلبات المزامنة
- API versioning للتعامل مع البيانات القديمة

#### Features
- تخزين مؤقت للصفحات الرئيسية
- حفظ البيانات محلياً
- مزامنة تلقائية عند عودة الاتصال
- مؤشر حالة الاتصال

---

## 4️⃣ حساب مرافق (أحد من الأسرة)

### الوصف
السماح لأفراد العائلة بمتابعة ومساعدة المريض.

### التنفيذ

#### Database Schema
```sql
- companions table: معلومات المرافقين
- companion_permissions table: الصلاحيات
- shared_medications table: الأدوية المشاركة
```

#### Backend Components
- `CompanionService` - إدارة المرافقين
- `CompanionInvitation` - دعوات المرافقين
- `CompanionNotification` - إشعارات للمرافقين

#### API Endpoints
- `POST /api/companions/invite` - دعوة مرافق
- `GET /api/companions` - قائمة المرافقين
- `PUT /api/companions/{id}/permissions` - تحديث الصلاحيات
- `GET /api/patients` - للمرافق: قائمة المرضى المتابعين

---

## 5️⃣ مشاركة خطة العلاج

### الوصف
إنشاء ومشاركة ملخص خطة العلاج.

### التنفيذ

#### Backend Components
- `TreatmentPlanExporter` - تصدير خطة العلاج
- PDF/Excel generators
- Link sharing مع انتهاء صلاحية

#### API Endpoints
- `GET /api/treatment-plan/export` - تصدير (PDF, Excel, JSON)
- `POST /api/treatment-plan/share` - إنشاء رابط مشاركة
- `GET /api/shared/treatment-plan/{token}` - عرض الخطة المشاركة

#### Features
- تصدير PDF منسق
- تصدير Excel للأطباء
- روابط مشاركة آمنة
- انتهاء صلاحية الروابط

---

## 6️⃣ بحث خارجي عن الدواء

### الوصف
روابط سريعة لمصادر موثوقة عن الأدوية.

### التنفيذ

#### Backend Components
- `MedicationInfoService` - روابط المصادر الموثوقة
- قائمة مصادر محددة مسبقاً

#### API Endpoints
- `GET /api/medicines/{id}/info-sources` - روابط المصادر

#### Features
- روابط لـ:
  - WebMD
  - Drugs.com
  - FDA
  - NHS
  - مصادر عربية موثوقة
- تنبيه: "هذه معلومات خارجية - استشر طبيبك"

---

## 7️⃣ تنبيهات وتحذيرات عامة

### الوصف
فحص ذكي للتعارضات والمشاكل المحتملة.

### التنفيذ

#### Backend Components
- `AlertService` - نظام التنبيهات
- `DoseConflictChecker` - فحص التعارضات
- `MissedDoseDetector` - كشف الجرعات المنسية

#### Alert Types
1. جرعات قريبة جداً من بعض (< 2 ساعات)
2. جرعات منسية
3. جرعات متأخرة بانتظام
4. أدوية متعددة في نفس الوقت
5. تناول الدواء في أوقات غير مناسبة

#### API Endpoints
- `GET /api/alerts` - قائمة التنبيهات النشطة
- `POST /api/alerts/{id}/dismiss` - إخفاء تنبيه
- `GET /api/alerts/history` - سجل التنبيهات

---

## خطة التنفيذ

### المرحلة 1: البنية التحتية (1-2 يوم)
- [ ] إنشاء Migrations للجداول الجديدة
- [ ] إنشاء Models الأساسية
- [ ] إعداد Service Classes

### المرحلة 2: المميزات الأساسية (2-3 أيام)
- [ ] تنبيه نفاد الدواء
- [ ] تنبيهات وتحذيرات عامة
- [ ] بحث خارجي عن الدواء

### المرحلة 3: المميزات المتقدمة (3-4 أيام)
- [ ] التذكير الذكي
- [ ] حساب مرافق
- [ ] مشاركة خطة العلاج

### المرحلة 4: PWA وOffline (2-3 أيام)
- [ ] Service Workers
- [ ] IndexedDB
- [ ] مزامنة البيانات

### المرحلة 5: الاختبار والتحسين (2 يوم)
- [ ] اختبار شامل
- [ ] تحسين الأداء
- [ ] توثيق

---

## التقنيات المستخدمة

### Backend
- Laravel Jobs & Queues
- Laravel Notifications
- Laravel Events
- Service Classes
- Repository Pattern

### Frontend
- Service Workers API
- IndexedDB
- Background Sync API
- Web Notifications API
- PWA Manifest

### Libraries
- Chart.js للرسوم البيانية
- PDF generation (DomPDF / mPDF)
- Excel export (PhpSpreadsheet)

---

## الأولويات

### عالية
1. ✅ تنبيه نفاد الدواء
2. ✅ تنبيهات وتحذيرات عامة
3. ✅ حساب مرافق

### متوسطة
4. ✅ مشاركة خطة العلاج
5. ✅ التذكير الذكي

### منخفضة (Nice to have)
6. ⚪ بحث خارجي
7. ⚪ PWA كامل

---

## ملاحظات تقنية

### الأمان
- التحقق من الصلاحيات في كل endpoint
- تشفير البيانات الحساسة
- Rate limiting للـ APIs
- Token expiration للمشاركة

### الأداء
- Caching للبيانات المتكررة
- Queue للمهام الثقيلة
- Database indexing
- Lazy loading

### التوافق
- دعم المتصفحات الحديثة
- Progressive enhancement
- Graceful degradation
- Mobile-first design

---

**آخر تحديث**: 2025-12-23
**الحالة**: قيد التنفيذ 🚧
